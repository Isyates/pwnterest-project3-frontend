//no imports

export default function Dev({dev}) {
        //receives a bunch of props from each iteration of the map of the dev array
        const {name, img, github, blurb} = dev
    return <div className="dev">
            
        {/* //renders those props */}

    </div>
}